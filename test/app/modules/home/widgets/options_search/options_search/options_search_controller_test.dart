import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:github_perfil/app/modules/home/widgets/options_search/options_search/options_search_controller.dart';
import 'package:github_perfil/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // OptionsSearchController optionssearch;
  //
  setUp(() {
    //     optionssearch = HomeModule.to.get<OptionsSearchController>();
  });

  group('OptionsSearchController Test', () {
    //   test("First Test", () {
    //     expect(optionssearch, isInstanceOf<OptionsSearchController>());
    //   });

    //   test("Set Value", () {
    //     expect(optionssearch.value, equals(0));
    //     optionssearch.increment();
    //     expect(optionssearch.value, equals(1));
    //   });
  });
}

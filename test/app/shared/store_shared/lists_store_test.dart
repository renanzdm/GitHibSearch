import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:github_perfil/app/shared/store_shared/lists_store.dart';
import 'package:github_perfil/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ListsStore lists;
  //
  setUp(() {
    //     lists = AppModule.to.get<ListsStore>();
  });

  group('ListsStore Test', () {
    //   test("First Test", () {
    //     expect(lists, isInstanceOf<ListsStore>());
    //   });

    //   test("Set Value", () {
    //     expect(lists.value, equals(0));
    //     lists.increment();
    //     expect(lists.value, equals(1));
    //   });
  });
}

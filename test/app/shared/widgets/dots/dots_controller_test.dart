import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:github_perfil/app/shared/widgets/dots/dots_controller.dart';
import 'package:github_perfil/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DotsController dots;
  //
  setUp(() {
    //     dots = AppModule.to.get<DotsController>();
  });

  group('DotsController Test', () {
    //   test("First Test", () {
    //     expect(dots, isInstanceOf<DotsController>());
    //   });

    //   test("Set Value", () {
    //     expect(dots.value, equals(0));
    //     dots.increment();
    //     expect(dots.value, equals(1));
    //   });
  });
}

import 'package:flutter_test/flutter_test.dart';

import 'package:github_perfil/app/shared/lists_service.dart';

void main() {
  ListsService service;

  setUp(() {
//    service = ListsService();
  });
//
  group('ListsService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<ListsService>());
//    });
//
  });
}
